name: Reference sheet smoke

on:
  pull_request:
  workflow_dispatch:

concurrency:
  group: reference-sheet-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Run reference sheet browser smoke
        run: |
          set -eu
          python3 -m http.server 8087 >/tmp/azaman-reference-http.log 2>&1 &
          server_pid=$!
          trap 'kill "$server_pid"' EXIT
          sleep 1
          chromium --headless=new --no-sandbox --disable-gpu --disable-dev-shm-usage --virtual-time-budget=8000 --dump-dom http://127.0.0.1:8087/tests/reference-sheet-smoke.html > /tmp/azaman-reference-smoke.html
          cat /tmp/azaman-reference-smoke.html
          grep -q 'REFERENCE SHEET SMOKE COMPLETE' /tmp/azaman-reference-smoke.html
