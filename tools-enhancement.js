(() => {
  'use strict';
  const FORMULAS = `<div class="reference-toolbar" role="toolbar" aria-label="Reference sheet controls"><div class="reference-zoom-controls" aria-label="Reference sheet zoom"><button type="button" class="btn" data-reference-action="zoom-out" aria-label="Zoom out reference sheet">−</button><span class="reference-zoom-value" aria-live="polite">100%</span><button type="button" class="btn" data-reference-action="zoom-in" aria-label="Zoom in reference sheet">+</button><button type="button" class="btn" data-reference-action="reset" aria-label="Reset reference sheet view">Reset</button></div><button type="button" class="btn" data-reference-action="compact" aria-pressed="false">Compact layout</button><span class="small">At higher zoom, drag inside the sheet to pan.</span></div><div class="reference-viewport" tabindex="0" aria-label="Reference sheet viewport"><div class="reference-sheet-content"><h4>Reference</h4><section><strong>Circle</strong><p>Area: A = πr²</p><p>Circumference: C = 2πr</p></section><section><strong>Rectangle</strong><p>Area: A = lw</p></section><section><strong>Triangle</strong><p>Area: A = ½bh</p></section><section><strong>Right triangle</strong><p>c² = a² + b²</p></section><section><strong>Special Right Triangles</strong><p>30°–60°–90°: short leg = x, long leg = x√3, hypotenuse = 2x</p><p>45°–45°–90°: legs = s, hypotenuse = s√2</p></section><section><strong>Rectangular prism</strong><p>V = lwh</p></section><section><strong>Cylinder</strong><p>V = πr²h</p></section><section><strong>Sphere</strong><p>V = 4/3πr³</p></section><section><strong>Cone</strong><p>V = 1/3πr²h</p></section><section><strong>Pyramid</strong><p>V = 1/3lwh</p></section><section><p>Circle arc: 360° = 2π radians</p><p>Triangle angle sum: 180°</p></section></div></div>`;
  function visible(el) { return !!el && el.getClientRects().length > 0; }
  function closeOnEscape(event) { if (event.key !== 'Escape') return; const panel = document.querySelector('#calculatorPanel, #referencePanel'); if (!panel) return; panel.querySelector('.panel-head .icon-btn')?.click(); event.preventDefault(); }
  function makeDraggable(panel) { if (panel.dataset.dragReady === '1') return; const head = panel.querySelector('.panel-head'); if (!head) return; panel.dataset.dragReady = '1'; head.style.cursor = 'move'; let drag = null; head.addEventListener('pointerdown', (event) => { if (event.target.closest('button')) return; const rect = panel.getBoundingClientRect(); drag = { x: event.clientX, y: event.clientY, left: rect.left, top: rect.top }; try { head.setPointerCapture?.(event.pointerId); } catch (_) {} }); head.addEventListener('pointermove', (event) => { if (!drag) return; const left = Math.max(8, Math.min(window.innerWidth - panel.offsetWidth - 8, drag.left + event.clientX - drag.x)); const top = Math.max(8, Math.min(window.innerHeight - panel.offsetHeight - 8, drag.top + event.clientY - drag.y)); panel.style.left = `${left}px`; panel.style.top = `${top}px`; panel.style.right = 'auto'; }); head.addEventListener('pointerup', () => { drag = null; }); head.addEventListener('pointercancel', () => { drag = null; }); }
  function makeResizable(panel) { if (panel.dataset.resizeReady === '1') return; panel.dataset.resizeReady = '1'; const handle = document.createElement('button'); handle.type = 'button'; handle.className = 'resize-handle'; handle.setAttribute('aria-label', 'Resize calculator'); handle.setAttribute('title', 'Drag to resize'); handle.textContent = '↘'; panel.appendChild(handle); let drag = null; handle.addEventListener('pointerdown', (event) => { const rect = panel.getBoundingClientRect(); drag = { x: event.clientX, y: event.clientY, width: rect.width, height: rect.height }; try { handle.setPointerCapture?.(event.pointerId); } catch (_) {} event.preventDefault(); }); handle.addEventListener('pointermove', (event) => { if (!drag) return; panel.style.width = `${Math.max(280, Math.min(720, drag.width + event.clientX - drag.x))}px`; panel.style.maxHeight = `${Math.max(260, Math.min(window.innerHeight - 24, drag.height + event.clientY - drag.y))}px`; }); handle.addEventListener('pointerup', () => { drag = null; }); handle.addEventListener('pointercancel', () => { drag = null; }); }
  function enhanceReference(panel) { if (panel.dataset.referenceReady === '1') return; const head = panel.querySelector('.panel-head'); panel.dataset.dragReady = '0'; panel.innerHTML = `${head?.outerHTML || ''}${FORMULAS}`; panel.setAttribute('aria-labelledby', 'referencePanelTitle'); const title = panel.querySelector('.panel-head b'); if (title) { title.id = 'referencePanelTitle'; title.textContent = 'Reference sheet'; } panel.querySelector('#closeRef')?.setAttribute('aria-label', 'Close reference sheet'); panel.dataset.referenceReady = '1'; const viewport = panel.querySelector('.reference-viewport'); const content = panel.querySelector('.reference-sheet-content'); const zoomValue = panel.querySelector('.reference-zoom-value'); const compactButton = panel.querySelector('[data-reference-action="compact"]'); const state = { scale: 1, x: 0, y: 0, compact: false }; const render = () => { content.style.transform = `translate(${state.x}px, ${state.y}px) scale(${state.scale})`; zoomValue.textContent = `${Math.round(state.scale * 100)}%`; compactButton.setAttribute('aria-pressed', String(state.compact)); compactButton.textContent = state.compact ? 'Standard layout' : 'Compact layout'; viewport.classList.toggle('compact', state.compact); }; const applyZoom = (delta) => { const next = Math.max(.8, Math.min(2, +(state.scale + delta).toFixed(2))); if (next === state.scale) return; state.scale = next; state.x = 0; state.y = 0; render(); }; panel.querySelectorAll('[data-reference-action]').forEach((button) => button.addEventListener('click', () => { const action = button.dataset.referenceAction; if (action === 'zoom-in') applyZoom(.1); else if (action === 'zoom-out') applyZoom(-.1); else if (action === 'reset') { state.scale = 1; state.x = 0; state.y = 0; render(); } else if (action === 'compact') { state.compact = !state.compact; render(); } })); let pan = null; viewport.addEventListener('pointerdown', (event) => { if (state.scale <= 1 || event.target.closest('button')) return; pan = { x: event.clientX, y: event.clientY, ox: state.x, oy: state.y }; viewport.setPointerCapture?.(event.pointerId); viewport.classList.add('panning'); }); viewport.addEventListener('pointermove', (event) => { if (!pan) return; state.x = pan.ox + event.clientX - pan.x; state.y = pan.oy + event.clientY - pan.y; render(); }); const stopPan = () => { pan = null; viewport.classList.remove('panning'); }; viewport.addEventListener('pointerup', stopPan); viewport.addEventListener('pointercancel', stopPan); panel.querySelector('#closeRef')?.addEventListener('click', () => panel.remove()); makeDraggable(panel); render(); panel.querySelector('[data-reference-action="zoom-in"]')?.focus(); }
  function installHighlightStyle() { if (document.getElementById('azmHighlightStyle')) return; const style = document.createElement('style'); style.id = 'azmHighlightStyle'; style.textContent = '.azm-saved-highlight{background:var(--color-warning);color:var(--color-text);padding:0 .04em}'; document.head.appendChild(style); }
  function wrapHighlightInTextNode(node, text) { if (!text || !node.nodeValue) return false; const source = node.nodeValue; const start = source.indexOf(text); if (start < 0) return false; const end = start + text.length; const before = source.slice(0, start); const after = source.slice(end); const mark = document.createElement('mark'); mark.className = 'azm-saved-highlight'; mark.textContent = text; const parent = node.parentNode; if (!parent) return false; if (before) parent.insertBefore(document.createTextNode(before), node); parent.insertBefore(mark, node); if (after) parent.insertBefore(document.createTextNode(after), node); parent.removeChild(node); return true; }
  function restoreSavedHighlights() {
    installHighlightStyle();
    const containers = document.querySelectorAll('.source-panel .passage, .question-card .q-prompt');
    const saved = [...document.querySelectorAll('.annotation-list span')]
      .map((el) => el.textContent.replace(/^“|”$/g, '').trim())
      .filter(Boolean);
    containers.forEach((container) => {
      if (container.dataset.highlightsRestored === '1') return;
      for (const text of saved) {
        const walker = document.createTreeWalker(container, NodeFilter.SHOW_TEXT);
        const nodes = [];
        let node;
        while ((node = walker.nextNode())) nodes.push(node);
        for (const candidate of nodes) {
          if (candidate.parentElement?.closest('mark')) continue;
          if (wrapHighlightInTextNode(candidate, text)) break;
        }
      }
      container.dataset.highlightsRestored = '1';
    });
  }
  function enhance(panel) { if (!visible(panel)) return; panel.setAttribute('role', 'dialog'); panel.setAttribute('aria-modal', 'true'); if (panel.id === 'referencePanel') { enhanceReference(panel); return; } makeDraggable(panel); if (panel.id === 'calculatorPanel') { panel.setAttribute('aria-labelledby', 'calculatorPanelTitle'); const title = panel.querySelector('.panel-head b'); if (title) title.id = 'calculatorPanelTitle'; const close = panel.querySelector('#closeCalc'); close?.setAttribute('aria-label', 'Close calculator'); makeResizable(panel); if (panel.dataset.focusReady !== '1') { panel.dataset.focusReady = '1'; panel.querySelector('#calcDisplay')?.focus(); } } }
  document.addEventListener('keydown', closeOnEscape, true);
  const observer = new MutationObserver(() => { enhance(document.getElementById('calculatorPanel')); enhance(document.getElementById('referencePanel')); restoreSavedHighlights(); });
  if (document.getElementById('app')) observer.observe(document.body, { childList: true, subtree: true });
  restoreSavedHighlights();
})();
